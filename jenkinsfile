pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/yovel554/hello-world.git'
            }
        }
        stage('Build') {
            steps {
                sh 'touch gever.txt'
            }
        }
    }
}
